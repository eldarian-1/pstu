<UserControl x:Class="WpfUi.Blocks.Tables.SubjectTable"
             x:Name="SubjectTableItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ListView Name="SubjectList">
        <ListView.View>
            <GridView>
                <GridViewColumn DisplayMemberBinding="{Binding Path=SubjectId}">ID</GridViewColumn>
                <GridViewColumn DisplayMemberBinding="{Binding Path=Name}">Наименование</GridViewColumn>
            </GridView>
        </ListView.View>
        <ListView.ItemTemplate>
            <DataTemplate>
                <StackPanel Margin="5">
                    <TextBlock FontSize="16" Text="{Binding Path=SubjectId}" HorizontalAlignment="Center" />
                    <TextBlock FontSize="16" Text="{Binding Path=Name}" HorizontalAlignment="Center" />
                </StackPanel>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</UserControl>
