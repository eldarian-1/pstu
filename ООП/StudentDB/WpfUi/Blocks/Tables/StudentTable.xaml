<UserControl x:Class="WpfUi.Blocks.Tables.StudentTable"
             x:Name="StudentTableItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ListView Name="StudentList">
        
        <ListView.View>
            <GridView>
                <GridViewColumn DisplayMemberBinding="{Binding Path=StudentId}">ID</GridViewColumn>
                <GridViewColumn DisplayMemberBinding="{Binding Path=FirstName}">Имя</GridViewColumn>
                <GridViewColumn DisplayMemberBinding="{Binding Path=LastName}">Фамилия</GridViewColumn>
            </GridView>
        </ListView.View>
        
        <ListView.ItemTemplate>
            <DataTemplate>
                <StackPanel Margin="5">
                    <TextBlock FontSize="16" Text="{Binding Path=StudentId}" HorizontalAlignment="Center" />
                    <TextBlock FontSize="16" Text="{Binding Path=FirstName}" HorizontalAlignment="Center" />
                    <TextBlock FontSize="16" Text="{Binding Path=LastName}" HorizontalAlignment="Center" />
                </StackPanel>
            </DataTemplate>
        </ListView.ItemTemplate>
        
    </ListView>
</UserControl>
